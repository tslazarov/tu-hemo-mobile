<Page navigatingTo="onNavigatingTo"
    actionBarHidden="true"
    class="page"
    loaded="onPageLoaded"
    xmlns="http://schemas.nativescript.org/tns.xsd">
    <GridLayout rows="180,*,auto">
        <StackLayout row="0">
            <ListPicker id="languagePicker" items="{{ languages }}" selectedIndex="{{ selectedLanguage }}" style="margin:20% 25% 0% 25%;" loaded="onListPickerLoaded" ></ListPicker>           
        </StackLayout>
        <StackLayout row="1" class="p-t-15" id="LoginPanel" backgroundColor="yellow">
            <TextField class="m-t-10 m-b-10 m-l-15 m-r-15" hint="{{ emailHint }}" keyboardType="email" autocorrect="false" autocapitalization="none"
                text="{{ email }}"></TextField>
            <TextField class="m-t-10 m-b-10 m-l-15 m-r-15" hint="{{ passwordHint }}" secure="true" autocorrect="false" autocapitalization="none"
                text="{{ password }}"></TextField>
            <Button class="btn btn-primary" text="{{ signIn }}" tap="onSignInButtonTap"></Button>
        </StackLayout>
        <StackLayout row="2" backgroundColor="red">
            <Label text="asd"/>
        </StackLayout>
    </GridLayout>
</Page>